# 项目统一规则入口
#
# 注意：`.rules` 文件会被 Codex 按 Starlark 解析。治理说明必须写成注释，
# 不要写 Markdown 正文，否则会触发 rules 加载失败。
#
# 本文件是 `.codex/rules/` 的入口。Agent 读取 `AGENTS.md` 后，必须继续读取并遵守：
#
# 1. `.codex/rules/architecture.rules`
# 2. `.codex/rules/verify.rules`
# 3. `AGENTS.override.md` 中声明的项目覆盖规则、治理命令和 base-ref 策略（如果存在）
#
# 执行顺序：
#
# - Analyze 阶段先用 `architecture.rules` 判断是否命中架构阈值。
# - Analyze 阶段同时判断用户意图或旧债信号是否触发“替换旧设计模式”。
# - 用户报告重复故障、用户可见页面无错误详情、生成任务多轨、提示词/schema 双轨、
#   历史任务或旧 API 残留时，Analyze 阶段必须先还原产品症状、证据链和设计异味，
#   不得先站在旧实现上解释或申辩。
# - 任务涉及 AI / 自动化输出契约、结构化输出框架、typed 输出模型或 LLM 输出格式时，
#   Analyze 阶段必须判断当前结构契约是否已有 typed model 或等价单一真相来源；
#   不得默认再添加 schema_version、弱 dict 主流程或 normalizer 双轨作为“保险”。
# - 任务涉及 formatter、runner、worker、orchestrator、store completion 或等价流水线时，
#   Analyze 阶段必须核查 `source output -> structured output -> projected output -> boundary payload`
#   的类型注解和变量命名，不得把结构化输出泛化为 `BaseModel` 或继续命名为 `raw_output`。
# - 任务涉及 Codex 配置治理、skill 优化、AGENTS/rules/hooks/project-skill 噪声或规则重复时，
#   Analyze 阶段必须使用 `.codex/skills/codex-config-optimizer/SKILL.md`，先做删重、迁移和脚本化判断，
#   不得默认继续追加常驻配置。
# - 任务涉及 env 文件选择、容器/本机模式、Docker volume、观测地址、本机调试或后台 Agent 模型凭据时，
#   只有项目覆盖层或可选 skill 声明了 runtime/env 专项治理，才按该专项矩阵执行；
#   默认通用层只要求说明 Consumer x Mode x Boundary，不得把“被选择的 env 文件”描述成“覆盖”。
# - 任务涉及 AI / 自动化输出契约时，Analyze 阶段必须先做字段所有权判断：
#   后端已能从任务输入、DB row、route 参数、store context 或调度上下文确定的字段，
#   不得进入模型输出契约，也不得让 LLM 复述后再作为权威值。
# - Plan 阶段写明阈值命中情况、处理方式、验证方式、Docker 卷路径；触发替换旧设计模式时，
#   还必须列出旧设计的删除、迁移、保留清单和公开契约/配置/数据/文档/测试/内部兼容层处理方式。
# - Plan 阶段如涉及 AI / 自动化输出契约，必须列出 backend-owned、agent-owned、
#   boundary-owned 字段矩阵，并说明 prompt、结构化契约、OutputModel、projection 和测试如何落位。
# - Plan 阶段如涉及用户可见工作流、后台任务、结构化输出、typed model、数据投影、
#   API 契约或生成类型，必须产出契约整改预检五项：产品症状证据链、字段所有权矩阵、
#   执行动作矩阵、治理风险预检和验证矩阵；缺少任一项时不得进入 Execute。
# - Execute 阶段普通小改只做已确认计划内的最小必要改动；替换旧设计模式下，
#   按已确认的新架构边界删除、迁移或统一旧实现。
# - Verify 阶段按 `verify.rules` 做功能验证和架构验证。
# - 治理检查默认使用项目覆盖层声明的正式阻断模式。warn、dry-run 或只读审计类命令
#   只用于 Analyze 阶段观察，不得作为 Verify 通过标准。
#
# 如果项目存在项目级方法论文档，且任务属于长程重构、跨层 schema 收口或治理硬门沉淀，
# Analyze/Plan 阶段必须读取该文件，并把其中适用的 lenses 体现在计划和验证矩阵中。
#
# 优先级：
#
# - 代码质量 > 新设计/框架/架构 > 旧模式兼容或保留。
# - 产品可见失败优先按契约、状态机、数据投影、前后端展示链路和治理缺口排查；
#   只有证据证明是孤立实现错误时，才按普通 bug 处理。
# - typed structured output 已能表达结构契约时，内部主流程优先传递模型实例或等价 typed object；
#   JSON dump 只允许出现在 DB、HTTP、文件、日志或观测等明确边界。
# - 结构化输出主链路的返回类型必须是具体输出模型或项目约定的 typed interface；
#   后端投影后的输出必须是具体投影模型或 typed interface；两者不得用 `BaseModel` 混写。
# - LLM 只对其生成的业务语义负责；job_id、workflow_id、case ids、时间戳、scope、
#   provenance、执行上下文和其他可确定性获取的系统字段必须由后端注入或覆盖。
# - “精准改动”“匹配现有风格”“外部行为不变”是普通小改默认值；
#   触发替换旧设计模式后，不得用这些原则默认保留旧 facade、兼容 shim、历史路径或重复实现。
#
# 如果规则之间存在冲突，以更具体、更安全、更能防止技术债继续扩大的规则为准。
