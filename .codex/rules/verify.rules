# 验证规则
#
# 注意：`.rules` 文件会被 Codex 按 Starlark 解析。治理说明必须写成注释，
# 不要写 Markdown 正文，否则会触发 rules 加载失败。
#
# ## 功能验证
#
# - Python 运行优先使用仓库本地 `.venv/bin/python`；项目未配置 `.venv` 时，在总结中说明替代解释器和风险。
# - 后端变更默认运行相关 `pytest`；跨模块变更运行项目声明的完整测试或等价命令。
# - 前端变更默认运行项目声明的构建命令，必要时补充浏览器验证。
# - 产品主流程改动必须先声明 UI、API、后台任务、数据投影、结构化输出和测试映射；如项目配置了
#   主流程 manifest 或覆盖清单，必须同步对应场景绑定。
# - 本地开发内循环优先目标测试或主流程测试；全量 coverage 适合作为 CI、提交前或发版前硬门。
#   不得把 coverage 百分比当作主流程场景覆盖的替代品。
# - 用户可见页面状态变更必须验证空态、成功态和失败态错误详情；只跑后端存储测试不足以证明 UI 契约成立。
# - Docker 持久化相关变更必须检查项目覆盖层声明的默认运行态路径，以及历史卷路径的迁移/兼容策略。
# - Runtime/env 相关变更必须验证 mode 选择、env 示例同构/差异、私有 env ignored 状态、
#   模型凭据不进示例文件、启动日志字段和 README/docs 术语一致性。影响后台任务时运行项目主流程测试；
#   发版或提交前再升级到项目完整硬门。
#
# ## 中段治理 checkpoint
#
# 涉及用户可见工作流、后台任务、结构化输出、typed model、数据投影、API 契约或生成类型的
# 非琐碎变更，不得等到最终完整测试才运行治理。实现中段完成主要 schema/store/formatter 改动后，
# 必须先执行项目覆盖层声明的正式治理命令，并对本轮新增或修改的 Python 文件运行项目声明的
# Ruff、format 或 typecheck。
#
# 如果中段 checkpoint 发现新增弱 dict 边界、长函数、超限文件、未归属 payload、
# schema 双轨或 import/type 问题，必须先修复结构问题，再继续补功能或测试。
#
# ## 架构验证
#
# 完成后必须说明：
#
# - 是否新增超过阈值的代码文件、函数、类或路由模块。
# - 是否新增超过 40 行的重复代码块。
# - 是否新增生命周期状态字面量分散、缺转移表的状态机、schema 双轨、跨事务中间态或硬编码分类字符串。
# - 如触发替换旧设计模式，是否验证旧 facade、兼容 shim、历史路径、重复实现、不可达分支或过期 API 已按计划删除、迁移或明确保留。
# - 如公开 API、配置/env、持久化数据或旧入口被改变，是否补充迁移说明、文档更新和针对新契约的测试。
# - 是否同步了 README、docs、生成类型、公开契约或环境变量示例。
# - 是否运行了项目覆盖层声明的治理硬门；项目未配置时必须说明替代验证与缺口。
# - 是否影响主流程覆盖清单；如影响，是否已更新 manifest / 覆盖清单并运行对应主流程测试。
# - 如治理输出 `BASELINE` 或类似提示，是否确认它表示既有超限未增长；如输出 `FAIL`，必须先修复。
#
# ## 旧路径清零验证
#
# 触发替换旧设计模式后，Verify 阶段必须对旧 symbol、旧路由、旧任务类型、旧 prompt、旧 schema
# version、旧前端入口和旧生成类型做文本检索，并把结果分类：
#
# - 活跃运行时代码、路由、服务、前端交互或生成类型中的旧入口必须清零。
# - 负向测试、历史数据清理、只读迁移解释和文档中的旧名称可以保留，但必须说明用途。
# - 兼容点如果仍会被主流程调用，不能归类为历史兼容，必须回到替换旧设计计划继续清理。
#
# 修改公开 API 或生成契约后，必须验证生成物不再暴露已删除旧入口。
#
# ## Typed Output 验证
#
# 涉及结构化输出、后台任务输出或 typed 输出模型时，Verify 阶段必须确认：
#
# - 成功路径返回的是具体 typed 输出模型或项目约定的 typed interface，内部主流程没有立即退化为弱 dict。
# - typed 校验失败会进入任务失败状态，并投影到错误详情、API response 和用户可见页面。
# - 新主流程没有新增不必要的输出 schema version 契约层。
# - 任务类型、profile、prompt builder、结构化输出契约和 OutputModel 的来源是集中注册表或项目约定的单一真相来源。
# - JSON dump 只出现在 DB、HTTP、文件、日志或观测边界；如在内部出现，必须说明原因。
# - prompt、结构化输出契约和 OutputModel 只要求 agent-owned 输出字段；backend-owned 字段只作为输入上下文或后端投影值。
# - touched 任务类型至少有 1 个 hostile backend-owned 字段污染测试；模型输出错误 job_id、
#   workflow_id、case ids、时间戳、scope 或 provenance 时，最终投影必须使用后端权威值。
# - 检索确认主链路没有新增 `BaseModel` 类型擦除、弱 dict 边界或把结构化输出继续命名为 `raw_output` 的路径。
# - 修改 formatter、runner、worker、orchestrator、store completion 或 typed output 注册表时，运行项目声明的
#   typecheck 或等价 Ruff + 类型检查；如暂未纳入完整测试，必须单独说明结果。
#
# ## 长程重构验证矩阵
#
# 长程重构、跨层 schema 收口、数据投影层变更、API 契约或生成类型变更，
# 默认必须覆盖以下验证；不适用时要在总结中说明原因：
#
# - 项目声明的正式治理硬门。
# - 后端相关测试；跨模块或治理变更运行项目完整测试。
# - 产品主流程测试；项目如提供专门主流程测试命令，主流程改动必须运行。
# - API 契约导出和生成类型同步。
# - 前端构建；涉及浏览器可见页面时补真实浏览器 smoke。
# - 涉及持久化投影、历史 schema 或 Docker 卷数据时，使用真实历史数据验证列表、详情和关键 UI。
# - 阶段结束时审计是否还有新增弱 dict 边界、schema 双轨、旧 shim 或生成物漂移。
#
# 非琐碎代码变更必须运行项目配置的正式治理检查。warn、dry-run 或只读审计类模式只能用于
# Analyze 阶段观察，不能作为 Verify 通过标准。只有项目未配置硬门、环境缺失、依赖缺失
# 或脚本自身不可用时才可不运行，且必须把该验证项标记为未通过或未配置并说明后续恢复路径。
#
# ## 测试纵深
#
# - 生命周期状态字段变更：至少 1 个非法状态转移或非法输入测试。
# - 并发资源变更：至少 1 个重复执行、竞争或部分失败测试。
# - 外部输入变更：至少 1 个异常、恶意或越权输入测试。
# - 只有 happy path 时，必须说明剩余风险。
